---
// The old "Features & Chat Organisation" page described Features as the primary
// unit of work. That model is gone — the feed and its threads replaced it — so
// this path now redirects to its successor rather than 404ing on an indexed URL.
return Astro.redirect('/docs/feed/', 301);
---
