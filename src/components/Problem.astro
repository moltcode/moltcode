---
---

<section class="py-24 px-6">
  <div class="max-w-5xl mx-auto">
    <!-- Section intro -->
    <div class="text-center mb-16">
      <div class="section-badge">
        <span class="section-badge-dot"></span>
        The Problem
      </div>
      <h2 class="text-3xl md:text-5xl font-bold text-theme-primary mb-4">
        Raw power<br /><span class="gradient-text">without a harness.</span>
      </h2>
    </div>

    <!-- Bottleneck visualization -->
    <div class="bottleneck-callout">
      <div class="bottleneck-bars">
        <div class="bottleneck-row">
          <span class="bottleneck-label">Today</span>
          <div class="bottleneck-bar">
            <div class="bar-segment bar-fast" style="flex: 1;">
              <span>Code generation</span>
            </div>
            <div class="bar-segment bar-slow" style="flex: 4;">
              <span>Planning, context, orchestration</span>
            </div>
          </div>
        </div>
        <div class="bottleneck-row">
          <span class="bottleneck-label">With Molt Code</span>
          <div class="bottleneck-bar">
            <div class="bar-segment bar-fast" style="flex: 1;">
              <span>Code generation</span>
            </div>
            <div class="bar-segment bar-reduced" style="flex: 1.5;">
              <span>Orchestration</span>
            </div>
          </div>
        </div>
      </div>
      <p class="bottleneck-insight">
        Models get faster every month. The bottleneck was never the engine &mdash; it&apos;s the lack of a harness. <span class="gradient-text">We built one.</span>
      </p>
    </div>

    <!-- Before / After split -->
    <div class="grid md:grid-cols-2 gap-6 md:gap-0">
      <!-- Before -->
      <div class="before-panel">
        <div class="panel-label">
          <div class="panel-label-icon before-icon">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
          </div>
          Without orchestration
        </div>
        <div class="panel-items">
          <div class="panel-item">
            <span class="item-num">01</span>
            <div>
              <p class="item-title">One prompt at a time</p>
              <p class="item-desc">You prompt, agent implements, you review. For every task. Every file. Every feature.</p>
            </div>
          </div>
          <div class="panel-item">
            <span class="item-num">02</span>
            <div>
              <p class="item-title">No dependency awareness</p>
              <p class="item-desc">Agents work in isolation. They don&apos;t know what other agents did or what needs to happen first.</p>
            </div>
          </div>
          <div class="panel-item">
            <span class="item-num">03</span>
            <div>
              <p class="item-title">Manual breakdown</p>
              <p class="item-desc">You&apos;re the planner, the PM, and the reviewer. The agent only does the last mile.</p>
            </div>
          </div>
          <div class="panel-item">
            <span class="item-num">04</span>
            <div>
              <p class="item-title">Context starts from zero</p>
              <p class="item-desc">Every new session loses the requirements, the scope, the constraints. You re-explain every time.</p>
            </div>
          </div>
        </div>
      </div>

      <!-- After -->
      <div class="after-panel">
        <div class="panel-label">
          <div class="panel-label-icon after-icon">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
          With Molt Code
        </div>
        <div class="panel-items">
          <div class="panel-item">
            <span class="item-num accent">01</span>
            <div>
              <p class="item-title">Spec once, build everything</p>
              <p class="item-desc">Write one PRD. The pipeline creates issues, assigns agents, and ships features.</p>
            </div>
          </div>
          <div class="panel-item">
            <span class="item-num accent">02</span>
            <div>
              <p class="item-title">Dependency-aware execution</p>
              <p class="item-desc">Issues are tracked with blockers and execution order. Agents build in the right sequence.</p>
            </div>
          </div>
          <div class="panel-item">
            <span class="item-num accent">03</span>
            <div>
              <p class="item-title">Agents plan and execute</p>
              <p class="item-desc">The Planner researches your codebase and writes the plan. Resolvers implement it. You review.</p>
            </div>
          </div>
          <div class="panel-item">
            <span class="item-num accent">04</span>
            <div>
              <p class="item-title">Persistent context</p>
              <p class="item-desc">PRDs, issues, and docs travel with every session. Agents always know what to build.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .section-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-accent);
    margin-bottom: 1rem;
    padding: 0.375rem 1rem;
    border-radius: 9999px;
    background: rgba(var(--color-accent-rgb), 0.08);
    border: 1px solid rgba(var(--color-accent-rgb), 0.15);
  }

  .section-badge-dot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--color-accent);
  }

  .before-panel,
  .after-panel {
    padding: 2rem;
    border-radius: 1.25rem;
  }

  .before-panel {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
  }

  .after-panel {
    background: rgba(var(--color-accent-rgb), 0.03);
    border: 1px solid rgba(var(--color-accent-rgb), 0.15);
  }

  @media (min-width: 768px) {
    .before-panel {
      border-radius: 1.25rem 0 0 1.25rem;
      border-right: none;
    }
    .after-panel {
      border-radius: 0 1.25rem 1.25rem 0;
      border-left: 2px solid rgba(var(--color-accent-rgb), 0.25);
    }
  }

  .panel-label {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--color-secondary);
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--color-border);
  }

  .panel-label-icon {
    width: 1.5rem;
    height: 1.5rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .before-icon {
    background: rgba(239, 68, 68, 0.15);
    color: #ef4444;
  }

  .after-icon {
    background: rgba(40, 200, 64, 0.15);
    color: #28c840;
  }

  .panel-items {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .panel-item {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .item-num {
    font-family: var(--font-family-mono);
    font-size: 0.6875rem;
    color: var(--color-secondary);
    opacity: 0.3;
    flex-shrink: 0;
    padding-top: 0.125rem;
  }

  .item-num.accent {
    color: var(--color-accent);
    opacity: 0.7;
  }

  .item-title {
    font-size: 0.9375rem;
    font-weight: 500;
    color: var(--color-primary);
    margin-bottom: 0.125rem;
  }

  .item-desc {
    font-size: 0.8125rem;
    color: var(--color-secondary);
    line-height: 1.5;
  }

  /* Bottleneck visualization */
  .bottleneck-callout {
    max-width: 40rem;
    margin: 0 auto 3rem;
    padding: 1.5rem;
    border-radius: 1rem;
    border: 1px solid var(--color-border);
    background: var(--color-surface);
  }

  .bottleneck-bars {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .bottleneck-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .bottleneck-label {
    font-family: var(--font-family-mono);
    font-size: 0.625rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-secondary);
    width: 6.5rem;
    flex-shrink: 0;
    text-align: right;
  }

  .bottleneck-bar {
    display: flex;
    flex: 1;
    height: 2.25rem;
    border-radius: 0.5rem;
    overflow: hidden;
    gap: 2px;
  }

  .bar-segment {
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 0.375rem;
    transition: flex 0.6s ease;
  }

  .bar-segment span {
    font-family: var(--font-family-mono);
    font-size: 0.5625rem;
    font-weight: 500;
    letter-spacing: 0.02em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    padding: 0 0.5rem;
  }

  .bar-fast {
    background: rgba(var(--color-accent-rgb), 0.15);
    border: 1px solid rgba(var(--color-accent-rgb), 0.25);
  }

  .bar-fast span {
    color: var(--color-accent);
  }

  .bar-slow {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.2);
  }

  .bar-slow span {
    color: #ef4444;
    opacity: 0.8;
  }

  .bar-reduced {
    background: rgba(var(--color-accent-rgb), 0.08);
    border: 1px solid rgba(var(--color-accent-rgb), 0.15);
  }

  .bar-reduced span {
    color: var(--color-accent);
    opacity: 0.6;
  }

  .bottleneck-insight {
    margin-top: 1rem;
    text-align: center;
    font-size: 0.8125rem;
    color: var(--color-secondary);
    line-height: 1.5;
  }

  @media (max-width: 480px) {
    .bottleneck-label {
      width: 4.5rem;
      font-size: 0.5625rem;
    }

    .bar-segment span {
      font-size: 0.5rem;
    }

    .bottleneck-callout {
      padding: 1rem;
    }
  }
</style>